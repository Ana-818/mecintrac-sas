---
import DoubleTitle from "@components/ui/DoubleTitle.astro";
import { Icon } from 'astro-icon/components'
---
<section class="section-container mb-8">
  <DoubleTitle blackText={"PORQUE ESCOGER"} blueText={"MECINTRAC"} />
  <p class="mx-auto text-justify text-lg text-black md:w-1/2">Desde 2008, Mecintrac ha sido un referente en soluciones metalmecánicas de alta precisión, destacando por nuestro compromiso con la calidad y puntualidad. Combinamos tecnología de vanguardia con un equipo técnico altamente capacitado para garantizar resultados excepcionales en cada proyecto.</p>

  <div class="flex flex-col md:flex-row justify-center items-center gap-4 mt-5">
    <div class="flex flex-row items-center justify-start bg-blue rounded-xl gap-1 md:gap-2 px-4 py-3 benefit-item">
      <Icon name="mdi:check-decagram-outline" class="text-white size-8" />
      <p class="text-white text-lg">Calidad</p>
    </div>
    <div class="flex flex-row items-center justify-start bg-white rounded-xl gap-1 md:gap-2 px-4 py-3 benefit-item">
      <Icon name="mdi:clock-time-four-outline" class="text-blue size-8" />
      <p class="text-blue text-lg">Puntualidad</p>
    </div>
    <div class="flex flex-row items-center justify-start bg-blue rounded-xl gap-1 md:gap-2 px-4 py-3 benefit-item">
      <Icon name="mdi:shield-check-outline" class="text-white size-8" />
      <p class="text-white text-lg">Confianza</p>
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const benefitItems = document.querySelectorAll('.benefit-item');
    let maxWidth = 0;

    benefitItems.forEach(item => {
      const itemWidth = (item as HTMLElement).offsetWidth;
      if (itemWidth > maxWidth) {
        maxWidth = itemWidth;
      }
    });
    benefitItems.forEach(item => {
      (item as HTMLElement).style.width = `${maxWidth}px`;
    });
  });
</script>
